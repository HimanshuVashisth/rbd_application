const Joi = require('joi');

/**
 * OrderId should be 5 digits only
 * No Alphabets
 *  */

/**
 * StoreId should be 3 digits only
 * No Alphabets
 *  */

// async function input(ordNumber, storeNumber) {

// }

// module.exports = input;
